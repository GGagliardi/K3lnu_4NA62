#include "../include/numerics.h"
#include "../include/Num_integrate_l4_decay_rate.h"

using namespace std;


int main(int narg, char** argv)
{
  
  if(narg != 1) {
    cout<<"Use ./Klnull"<<endl;
    exit(-1);
  }

  

 
  
  return 0;
}











